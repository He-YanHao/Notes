# 内置函数 Built-in Functions

## 输入输出

输入

```python
print("语句1","语句2")
```

会输出`语句1 语句2`，并在末尾自动换行。

使用`end = ""`取消换行，如：

```python
print("语句1","语句2",end = "")
```

输出

```python
user_input = input("提示:")
```

会输出()内的提示词，将键盘输入部分以字符串的形式保存在接受返回值的变量里。

## 处理数字

```
min()#获得最小值
max()#获得最大值
```

