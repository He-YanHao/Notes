# 连接网络

```
wpa_supplicant -B -i wlan0 -c /etc/wpa_supplicant/wpa_supplicant.conf
iw dev wlan0 link
```





执行以下命令来连接到手机的热点（假设你的热点名称是 MyHotspot，密码是 mypassword）

```
sudo nmcli dev wifi connect MyHotspot password mypassword
```

安装并启动ssh

```
192.168.10.227
```

