# 五层模型 vs 七层模型

七层模型（OSI）：

1 物理层
 2 数据链路层
 3 网络层
 4 传输层
 5 会话层
 6 表示层
 7 应用层

五层模型把：

-   会话层
-   表示层

合并进了应用层。

------

# 为什么常用五层模型？

因为：

-   更贴近真实 TCP/IP 协议栈
-   更适合实际工程理解
-   大多数系统实现都按这个分层

例如：

HTTP → TCP → IP → Ethernet → 网卡