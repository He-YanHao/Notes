

扫描当前文件夹大小并排序

```bat
Get-ChildItem -Directory | %{ $s=(Get-ChildItem $_.FullName -Recurse -File | Measure-Object Length -Sum).Sum; [PSCustomObject]@{Folder=$_.Name; Size_MB=[math]::Round(($s/1MB),2)} } | sort Size_MB -Desc
```

